# passwd-svelte

SvelteKit web client for Passwd (Cloudflare adapter). Hallmark theme: **Cobalt** · macrostructure: **Marquee Hero**.

```bash
npm install
npm run dev
```

Zero-knowledge UI: unlock + encrypt client-side via `passwd-crypto`; sync ciphertext through `passwd-server`.
