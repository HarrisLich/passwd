# passwd-extension

WXT MV3 extension. Reuse `passwd-crypto` and the same API contracts as `passwd-svelte`.

**Never** inject vault keys into the page world. Content scripts request fill payloads from the background worker after origin checks.

See workspace skill `passwd-extension`.
