{
	"name": "passwd-extension",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"description": "Passwd browser extension (WXT) — autofill + secure messaging",
	"scripts": {
		"dev": "wxt",
		"build": "wxt build",
		"zip": "wxt zip",
		"typecheck": "tsc --noEmit --pretty",
		"test": "vitest",
		"lint": "eslint .",
		"prepare": "node -e \"try{require('child_process').execSync('husky',{stdio:'ignore'})}catch(e){}\""
	},
	"devDependencies": {
		"@types/chrome": "^0.0.304",
		"jsdom": "^25.0.1",
		"typescript": "^5.8.2",
		"vitest": "^3.0.8",
		"wxt": "^0.19.29"
	},
	"dependencies": {
		"passwd-crypto": "*",
		"tldts": "^6.1.86",
		"zod": "^3.24.2"
	}
}
