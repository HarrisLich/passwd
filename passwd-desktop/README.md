# passwd-desktop

Lightweight Tauri wrapper for Passwd.

Planned layout:

```
passwd-desktop/
├── src-tauri/          # Rust shell, secure storage hooks
└── (UI from passwd-svelte)
```

Scaffold Tauri against `../passwd-svelte` when ready for native packaging.
