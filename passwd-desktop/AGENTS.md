# passwd-desktop

Tauri v2 shell around `passwd-svelte`. Do not duplicate vault crypto or API clients — reuse the web app + `passwd-crypto`.

Next step: `npm create tauri-app` / configure `src-tauri` to load the SvelteKit SPA build (or `vite dev` in development).
