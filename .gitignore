.DS_Store
*.log
.env
.env.*
!.env.example
# Not secret — public half of a pinned extension signing key, committed so every
# machine building passwd-extension gets the same chrome-extension://<id>.
!passwd-extension/.env.local
.dev.vars

node_modules
dist
coverage
.wrangler
.svelte-kit
.output
.wxt

# Local DBs / runtime data
.data/
*.db
*.db-*

# Desktop / native
**/src-tauri/target/

# Playwright / test artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Editor / OS
*.swp
*.swo
.idea/
.vscode/*
!.vscode/extensions.json
